# Cross-system tests

Unit/integration suites live beside their code (`backend/tests`, `edge/esp32/tests/host`,
`ai/tests`). This directory is reserved for system-level suites — hardware-in-the-loop,
load/soak and end-to-end latency (plans in docs/TESTING.md). None exist yet.
